#ifndef FILM_BUFFER_H
#define FILM_BUFFER_H

#include <Eigen/Dense>

class FilmBuffer {
public:
    /*


    // main.cppの`initFilm`の役割
    void initFilm();

    // main.cppの`resetFilm`の役割
    void resetFilm();






private:


    float* g_filmBuffer;         // 表示用バッファ (平均化された色)
    float* g_accumulationBuffer; // 色の合計値を蓄積
    int* g_countBuffer;        // サンプル数をカウント
*/

};

#endif // FILM_BUFFER_H
